import {describe,expect,it} from 'vitest';import {buildDiff,createSession,threeToUnity,unityToThree} from './core';import type {Palette,SceneExport,Transform} from './types'
const t:Transform={position:[1,2,3],rotation:[0,0,0,1],scale:[1,1,1]};const scene:SceneExport={schemaVersion:'1.0.0',kind:'holocity.scene-export',sceneName:'Test',unityScenePath:'Assets/Test.unity',exportedAt:new Date().toISOString(),exportMode:'whole',baseHash:'abc',entries:[{id:'e1',name:'L',editable:true,prefabPath:'Assets/L.prefab',transform:t},{id:'locked',name:'Terrain',editable:false,transform:t}]};const palette:Palette={schemaVersion:'1.0.0',kind:'holocity.palette',items:[]}
describe('contract safety',()=>{it('untouched is empty',()=>{const d=buildDiff(createSession(scene,palette),'2026-01-01T00:00:00.000Z');expect(d.modified).toEqual([]);expect(d.added).toEqual([]);expect(d.deleted).toEqual([])});it('coordinate conversion round trips',()=>expect(threeToUnity(unityToThree(t))).toEqual(t));it('+90 Unity Y survives conversion',()=>{const q=Math.SQRT1_2;const r:Transform={...t,rotation:[0,q,0,q]};expect(threeToUnity(unityToThree(r)).rotation).toEqual([0,q,0,q])});it('locked objects are absent from edit state',()=>expect(createSession(scene,palette).editable.map(x=>x.id)).toEqual(['e1']))})
